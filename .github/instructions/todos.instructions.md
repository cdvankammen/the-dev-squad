---
applyTo: '**'
---

<todos title="P0 Implementation: tool registry + session trimming" agentRequirement="Review steps frequently throughout the conversation and DO NOT stop between steps unless they explicitly require it. Keep the todos updated using the todo_write tool (do not edit this file).">
- [x] implement-tool-registry: Add central tool registry and replace ad-hoc normalization across shim, orchestrator, and API routes 🔴
  _Introduce a canonical registry mapping aliases to canonical tool IDs. Use registry in shim (parse/normalize), orchestrator, and API to ensure single source of truth. Keep handler mapping for shim dispatch and requiresApproval metadata for gating._
- [x] implement-session-trimmer: Replace naive session trimming with importance-scoring trimmer that preserves system prompts and tool results and summarizes older messages 🔴
  _Implement scoring rules to keep system prompts and tool results; summarize older messages when over budget; update session schema to include normalized_tool_call metadata._
- [x] provider-config-cache-invalidation: Add mtime-based caching and fs.watch invalidation for provider-config.json 🟡
  _Cache provider-config.json and invalidate on mtime change or fs.watch events; expose a force-reload API for manual refresh._
- [ ] enforce-request-tool-timeouts: Add consistent request and tool-call timeouts on client, server, and shim 🟡
  _Ensure HTTP_SHIM_REQUEST_TIMEOUT_MS applied everywhere and server endpoints respect timeouts; add graceful timeout handling and retries for tool calls._
- [x] replace-shell-glob-with-node-lib: Replace shell glob/grep usages in shim with Node 'fast-glob' and sanitize inputs 🟡
  _Remove shell calls to improve cross-platform compatibility and security; validate inputs and limit results._
- [ ] add-tests-and-run-smoke-suite: Add unit tests for tool normalization and session-resume; run typecheck and smoke e2e test suite 🔴
  _Extend existing test harness with normalization variants and resume scenarios; run npx tsc, node --check, and orchestrator smoke scripts._
- [x] implement-json-repair-layer: Add deterministic JSON repair & normalization layer and integrate into HTTP shim 🔴
  _Create scripts/json-repair.mjs and src/lib/jsonRepair.ts; try to repair and validate tool call arguments, emit clear repair errors back to agents when irreparable._
- [x] add-src-tool-registry-ts: Create TypeScript server-side tool registry at src/lib/toolRegistry.ts 🟡
  _Provide same canonical registry for server-side code to import; ensure parity with scripts/tool-registry.mjs._
- [x] integrate-jsonrepair-into-shim: Integrate parse/repair step into http-runner-shim before executing tools 🔴
  _Use parsePossiblyMalformedJson to parse tool function.arguments and to repair args extracted from text-based tool calls; if unrepairable, emit clear tool_result error and skip execution._
</todos>

<!-- Auto-generated todo section -->
<!-- Add your custom Copilot instructions below -->
